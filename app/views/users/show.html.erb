<h1><%= @user.username %></h1>

<table>
  <tr>
    <th>Service</th>
    <th>Username</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @credentials.each do |credential| %>
  <tr>
    <td><%= credential.service %></td>
    <td><%= credential.username %></td>
    <td><%= link_to 'View', user_credential_path(@user, credential) %></td>
  </tr>
<% end %>
</table>

<br />



<%= link_to 'New Credential', new_user_credential_path(@user) %>
